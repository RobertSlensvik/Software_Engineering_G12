package org.example.Repository;

import org.example.Model.*;
import java.util.ArrayList;
import java.util.HashMap;

public class CarRepository {

    HashMap<String, Car> showAvalibleCars();
    ArrayList<Car> showCar(String Car);

    /*Car getCar(String carKey);

    boolean isEmpty();

    HashMap<String, Car> showCar(String Car);
    void addCar(Car car);
    void removeCar(String carKey);
    void updateCar(String carKey, Car car);

    Hashmap<String, Car> userRentCar(User user);
    void userReturnCar(User user, Car car);


    Hashmap<String, Car> renterPrice(Store store);*/


}
